{
  "name": "gargantua-launcher",
  "private": true,
  "version": "0.1.0",
  "type": "module",
   "scripts": {
    "dev": "GDK_BACKEND=wayland WEBKIT_DISABLE_COMPOSITING_MODE=1 vite",
    "tauri:dev": "GDK_BACKEND=wayland WEBKIT_DISABLE_COMPOSITING_MODE=1 tauri dev",
    "build": "vite build",
    "tauri:bu ild": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "vite": "^7.3.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.9.1"
  }
}
